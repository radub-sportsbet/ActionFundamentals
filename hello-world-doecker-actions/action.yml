name: 'Hello World Docker Action'
description: 'Say hello to a user or the world'
inputs:
  who-to-greet:
    description: 'Who the greet'
    required: true
    default: 'world'

outputs:
  time:
    description: 'The time we saild hello.'

runs: 
  using: 'docker'
  image: 'DockerFile'
  args:
    - ${{ inputs.who-to-greet}}
